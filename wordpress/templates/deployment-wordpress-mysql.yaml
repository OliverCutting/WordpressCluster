apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.mysql.deployment.name }}
  labels:
    app: {{ .Values.mysql.deployment.labels.app }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.mysql.deployment.labels.app }}
      tier: {{ .Values.mysql.deployment.labels.tier }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: {{ .Values.mysql.deployment.labels.app }}
        tier: {{ .Values.mysql.deployment.labels.tier }}
    spec:
      containers:
      - image: {{ .Values.mysql.deployment.spec.image }}
        name: {{ .Values.mysql.deployment.labels.tier }}
        env:
        - name: {{ .Values.mysql.deployment.spec.env.name }}
          valueFrom:
            secretKeyRef:
              name: {{ .Values.mysql.deployment.spec.env.secretKeyRef.name }} 
              key: {{ .Values.mysql.deployment.spec.env.secretKeyRef.key }}
        ports:
        - containerPort: {{ .Values.mysql.deployment.port }}
          name: {{ .Values.mysql.deployment.labels.tier }}
        volumeMounts:
        - name: {{ .Values.mysql.deployment.spec.volumes.name }}
          mountPath: {{ .Values.mysql.deployment.spec.volumes.mountPath }}
      volumes:
      - name: {{ .Values.mysql.deployment.spec.volumes.name }}
        persistentVolumeClaim:
          claimName: {{ .Values.mysql.deployment.spec.volumes.claimName }}
